# Information-gain order, 2026-08-16

- Artifact ID: `issue-0076-information-gain`
- Issue: <https://github.com/EnzoTironi/OS/issues/76>
- Parent: <https://github.com/EnzoTironi/OS/issues/2>
- Research angle: dated execution order from the issue graph and sibling Wave A heads, not from issue numbering
- Decision states present: `hypothesis`, `supported`, `rejected`, `undetermined`
- Contract: Agent output contract in `docs/swarm-research-backlog.md`. Draft contract `docs/swarm-result-contract.md` on [PR 84](https://github.com/EnzoTironi/OS/pull/84).

This note is a snapshot. Recompute after review corrections land or after #72, #73 children, or #79 produce artifacts. Do not treat the ranking as a standing dashboard.

## 1. Question

Which unresolved research tasks still reduce architectural uncertainty the most, and which tasks are safe to run in parallel versus blocked on evidence?

Falsifiable claim under test:

> After Wave A first-pass notes exist on exclusive remotes, the next uncertainty reduction is review correction plus the remaining holes #72, #73 missing forms, and #79, not another first-pass on Party, Inventory, or ERPNext.

If a later merged note shows that first-pass claims already survive the issue 2 review gate, this ranking is wrong.

## 2. Sources

See `sources.md`. Docs and RFC-0001 were read on `origin/main` at `dc918a50e550d384d1e18a6f24424e6ed4595b9c`. Sibling notes were opened with `git show` only.

Material not examined is listed in `sources.md`.

## 3. Evidence

**E-001.** `docs/swarm-research-backlog.md` on `origin/main` says issue numbering is not priority. Wave A is foundation, domain, corpus, AGI #50 to #51, listed ops, and kill tests. Wave B is runtime, composition, toolchain, AGI #52 to #54, and #75. Wave C is #70 then #71, with #80 as a readiness gate.

**E-002.** Issue 76 names four reorder triggers. Role and Relator results that change Party, HR, or Procurement. #59 rejecting universal bitemporality, which changes storage research. #68 finding a platform that satisfies the thesis, which changes implementation research. Domain research that repeatedly needs a missing semantic form, which outranks runtime polish.

**E-003.** Issue 2 comment [5305027316](https://github.com/EnzoTironi/OS/issues/2#issuecomment-5305027316) states that deep review covered research PRs through #119, that none of #84 to #119 is merge-ready, and that each of those branches still had one commit. Recurring defects sit at the inference boundary. Record is not occurrence. Domain distinction is not kernel primitive. Product behavior is not universal law. Internal thesis text is not independent evidence.

**E-004.** `origin/main` `research/` still has only `README.md` and `reference-landscape.md`. Durable Wave A notes live on exclusive remotes and draft PRs. They are not merged evidence.

**E-005.** Remote first-pass trees exist for foundation #3 to #13 and #62, domains #14 to #31 and #67, corpus #32 to #38, AGI #50 to #51, kill #55 to #61 and #68, and ops #69 and #73. Open drafts run from [PR 84](https://github.com/EnzoTironi/OS/pull/84) through [PR 134](https://github.com/EnzoTironi/OS/pull/134).

**E-006.** No remote `cursor/issue-72-kill-cfd8` existed when remotes were listed. #72 still has no durable files.

**E-007.** `git show origin/cursor/issue-3-foundation-cfd8:research/identity-kinds-roles/issue-3-result.md` at `8b9ce1ee`. Folder state `hypothesis`. "Not a Kind" is `supported`. Native Relator is `undetermined`. RFC-0001 is unchanged.

**E-008.** `git show` of party `candidate-laws.md` at `c6434699` and HR `candidate-laws.md` at `8856f901`. Both already consume the "not a Kind" and "relationship earns identity" claims. HR says a native Relator is not earned by that domain alone.

**E-009.** `git show origin/cursor/issue-59-kill-cfd8:research/kill/fact-bitemporal/wave-a-issue-59.md` at `4f7057cb`. Over-generalized Fact and pervasive bitemporality are `rejected`. Issue 5 temporal `candidate-laws.md` at `a967d4de` independently marks universal bitemporal rows `rejected` as a domain law.

**E-010.** `git show origin/cursor/issue-68-kill-cfd8:research/kill/existing-platform/replace-os-report.md` at `10314410`. The claim that an inspected platform already satisfies the thesis cleanly is `rejected`. Adopting one inspected runtime as the MIT core is `rejected`. A later unopened platform could reopen the question. That last clause stays `hypothesis`.

**E-011.** `git show origin/cursor/issue-55-kill-cfd8:research/kill/unified-ontology/README.md` at `5f423357`. One enterprise vocabulary is `rejected`. One shared metamodel that executes many context ontologies is `hypothesis`. Whether that surviving reading still deserves the name "one executable ontology" is `undetermined` and belongs to synthesis.

**E-012.** `git show origin/cursor/issue-56-kill-cfd8:research/kill/primitives/README.md` at `b44575d3`. Relator as a native engine category is `rejected` on present evidence. An identifiable relationship-object remains `supported`. That conflicts with issue 3 leaving native Relator `undetermined`.

**E-013.** `git show origin/cursor/issue-73-ops-cfd8:research/ops/unknown-unknowns/map.md` at `9203bfce`. The scan ranks over-time performance, contingent occurrence, clinical statement kinds, deontic operators, retention versus history, and rights-as-resources above more ERP modules. It tells #79 to stress ranks 1 to 6, not to write a second discovery essay. It does not file the child issues.

**E-014.** Issue 77 requires messy real-company evidence. No de-identified company corpus is in this repository. The brief for this unit keeps #77 `blocked-needs-data`.

**E-015.** No runtime, toolchain, composition, or synthesis remotes exist. That matches the backlog wave split and the standing rule that Wave B recommendations and Wave C synthesis wait for Wave A evidence.

**E-016.** Kill first-passes on #57, #58, #60, and #61 already record folder verdicts on their remotes. Universal Action-only persist is `rejected`. A second semantic kernel is `rejected`. "No authority layer" is `rejected`. "Build from zero is inferior" is `rejected` for the semantic core. Those verdicts are still draft and sit under the same inference-boundary review.

## 4. Source artifacts

**SA-001.** GitHub issue numbers, PR numbers, and exclusive branch names. Coordination mechanics, not domain kinds.

**SA-002.** ERPNext DocTypes, Odoo models, Palantir Actions, Ontologiq effects, and similar product names inside sibling notes. Those names are evidence locations. They are not OS primitives.

**SA-003.** The Wave A, Wave B, and Wave C labels in `docs/swarm-research-backlog.md`. Useful scheduling buckets. Not ontological natures.

**SA-004.** Draft-PR review comments. Process state. A review finding is not a merged law.

**SA-005.** This folder's ranking table. An ops snapshot. Not a constitution rule.

## 5. Convergence

Independent sources already agree on several scheduling facts.

- Issue numbering is not roadmap order. Issue 2, the backlog, and issue 76 all say this. **Kind.** source-system artifact of the research program. **State.** `supported` as a coordination rule.
- Wave A first-pass coverage on remotes is broad. Foundation, domain, corpus, early AGI, and most kill tests have exclusive trees. **Kind.** source-system artifact. **State.** `supported` as a presence fact. Not as claim quality.
- Universal bitemporal rows and Fact-as-the-only-write-unit are under attack from #5 and #59 together. **Kind.** candidate law in those folders. **State.** `hypothesis` for OS until review accepts the kill. The presence of the attack is `supported`.
- Inspected platforms do not replace the thesis. #61 and #68 both refuse a donor core. **Kind.** candidate law in those folders. **State.** `hypothesis` until review. Presence of the refusal is `supported`.
- Customer, Supplier, and Employee as kinds are already refused by #3, #14, #28, and #55. Another first-pass on that collapse has little left to spend. **Kind.** domain evidence cited by those remotes. **State.** `supported` that the first-pass exists. The underlying party law stays whatever those folders say.

## 6. Divergence

**D-001. Native Relator.** Issue 3 leaves a native Relator `undetermined`. Issue 56 rejects the native category and keeps the identifiable-relationship pattern. Party and HR follow issue 3 more than issue 56. **Kind.** disagreement among draft notes. **State.** `undetermined` for OS. Highest remaining foundation disagreement that still moves Party, HR, and Procurement.

**D-002. One ontology.** The thesis diagram still draws one box over commerce through HR. Issue 55 rejects one enterprise vocabulary and leaves one metamodel `hypothesis`. Issue 2 review comment preserves several cross-cutting hypotheses and forbids inferring an RFC-0001 edit. **Kind.** thesis versus kill-test draft. **State.** `undetermined`. Do not start #70 to hide the split.

**D-003. Fact sort.** Issue 4 leaves Fact as a kernel type `undetermined`. Issue 59 rejects Fact as the fundamental write unit. Issue 56 rejects Fact as the only atom and leaves the kernel sort `undetermined`. **Kind.** disagreement among draft notes. **State.** `undetermined`. This is why #39 must not pick a fact store yet.

**D-004. Merge-ready versus written.** Workers have written a lot. Reviewers have accepted none of #84 to #119. Treating remote files as Wave A completion would maximize throughput and ignore the gate. **Kind.** source-system artifact of the swarm. **State.** `supported` as a process fact.

## 7. Candidate laws

See `candidate-laws.md`. The laws that change today's order are L-IG-01 through L-IG-08.

## 8. Counterexamples

**X-001.** A merged, review-clean #59 that restored universal bitemporality. Then #39 would rise again. Current remote #59 points the other way.

**X-002.** A #68 or later platform study that finds an MIT-compatible engine enforcing Action versus Event, unknown external outcomes, and historical explainability without a second semantic authority. Then implementation research would pivot. Current #68 rejects the inspected set.

**X-003.** A review that shows the first-pass notes already meet the issue 2 gate. Then this snapshot's "gain is spent" claim fails, and more first-pass writing could be justified.

**X-004.** De-identified company data landing in a permitted location. Then #77 leaves `blocked-needs-data` and can invalidate laws that only hold on clean reference systems.

**X-005.** Independent corpora showing that IFRS 15 over-time performance is only #16 plus #5 valid time. Then #73 rank 1 should not become a child issue. Issue 73 says that demonstration does not exist in its folder.

## 9. Runtime pressure

**R-001.** Do not start storage, transaction, or toolchain selection from RFC-0001 Fact rows or from a default bitemporal table. If #59 and #5 survive review, knowledge time is runtime-owned and valid time is optional per type. **Kind.** runtime consequence. **State.** `hypothesis` pending review.

**R-002.** Do not adopt Palantir, Open Foundry, Ontologiq, ObjectStack, Moqui, or Frappe as the MIT semantic core because #68 and #61 currently reject that move. Steal enforcement ideas only. **Kind.** runtime consequence. **State.** `hypothesis` pending review.

**R-003.** Do not encode `if objectType == "PurchaseOrder"` in a generic engine. That smell is already in `docs/open-questions.md` question 15 and in constitution rule 12. #83 can audit notes for leakage later. It should not wait for a runtime that does not exist, and it should not pretend a runtime exists. **Kind.** runtime consequence. **State.** `supported` as a research hygiene rule. Engine design stays `undetermined`.

**R-004.** Wave B physical evaluators for accounting or inventory remain allowed only as compiled Functions and Constraints if #58 survives review. A second business authority remains `rejected` in that draft. **Kind.** runtime consequence. **State.** `hypothesis`.

No storage engine, language, or compiler is selected here.

## 10. Open questions

These stay `undetermined`. They are not answers to `docs/open-questions.md`.

- Q-IG-01. After review corrections, which sibling folder verdicts still stand? Touches every Wave A draft.
- Q-IG-02. Does #72 show that a new engine adds more mappings than it removes? No artifact yet.
- Q-IG-03. Do #73 ranks 1 to 6 require new issues, or can #10, #15, #16, #18, and #67 absorb them? Issue 73 recommends children and also says to widen existing issues. Coordinator files. This unit does not.
- Q-IG-04. Native Relator or identifiable relationship-object only? Cite #3, #14, #28, #56. Do not invent a winner.
- Q-IG-05. One metamodel plus context ontologies, or a different primary artifact? Cite #55 and `docs/open-questions.md` question 1. Leave both `undetermined`.
- Q-IG-06. When does Wave A evidence exist in the sense that Wave B may recommend runtimes? Presence of remotes is not enough. The issue 2 gate says merge-ready notes are the bar. **State.** `hypothesis` for that bar.

`docs/open-questions.md` questions 1 through 23 remain as written on `origin/main`. This folder does not fill them.

## 11. Decision state

| Claim | State |
| --- | --- |
| Issue numbering is roadmap priority | `rejected` |
| Close research issues to raise throughput | `rejected` |
| Wave A first-pass remotes exist for most listed Wave A issues | `supported` as presence |
| Those remotes are merge-ready evidence | `rejected` for #84 to #119 per issue 2. `undetermined` for #120 to #134 |
| Next gain is more first-pass on #3 to #38 | `rejected` |
| Next gain is review correction plus #72, #73 missing forms, #79, and ops #78 #81 #82 | `hypothesis` |
| #59 currently rejects universal bitemporality on its remote | `supported` as a remote verdict. `hypothesis` as an OS law |
| #68 currently rejects inspected platforms as the core | `supported` as a remote verdict. `hypothesis` as an OS law |
| #77 can proceed without company data | `rejected` |
| Start Wave B runtime recommendations now | `rejected` |
| Start Wave C #70 or #71 now | `rejected` |
| Edit RFC-0001 from this snapshot | `rejected` |

Folder decision state. `hypothesis`.
