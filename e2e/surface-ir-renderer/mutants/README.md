# Surface IR renderer mutants

Named kills owned by this scenario:

- `button_value_is_authority`
- `renderer_emits_hidden_mutation`
- `group_card_leaks_forbidden_body`
- `silent_drop_critical_control`
- `provider_extension_bypasses_surface`

Honest build records each id in `artifacts/surface-ir-renderer/surface-ir-renderer.json` under `mutantsKilled`.
