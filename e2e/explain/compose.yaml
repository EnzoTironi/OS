services:
  postgres:
    image: postgres:18
    environment:
      POSTGRES_DB: zoen
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
    ports:
      - "127.0.0.1:55435:5432"
    volumes:
      - ../governed-action/init.sql:/docker-entrypoint-initdb.d/001-init.sql:ro
    healthcheck:
      test:
        - CMD-SHELL
        - pg_isready -U postgres -d zoen
      interval: 1s
      timeout: 3s
      retries: 30

  keycloak:
    image: quay.io/keycloak/keycloak:26.0.7
    command:
      - start-dev
      - --import-realm
    environment:
      KC_BOOTSTRAP_ADMIN_PASSWORD: admin
      KC_BOOTSTRAP_ADMIN_USERNAME: admin
      KC_HEALTH_ENABLED: "true"
    ports:
      - "127.0.0.1:58084:8080"
    volumes:
      - ../governed-action/.generated/realm.json:/opt/keycloak/data/import/realm.json:ro
    healthcheck:
      test:
        - CMD-SHELL
        - "bash -c ': > /dev/tcp/127.0.0.1/8080'"
      interval: 1s
      timeout: 3s
      retries: 90

  restate:
    image: docker.restate.dev/restatedev/restate:1.7.2
    environment:
      RESTATE_ADVERTISED_ADDRESS: http://restate:5122
      RESTATE_AUTO_PROVISION: "true"
      RESTATE_FORCE_NODE_ID: "1"
      RESTATE_NODE_NAME: restate
    extra_hosts:
      - host.docker.internal:host-gateway
    ports:
      - "127.0.0.1:55122:5122"
      - "127.0.0.1:58085:8080"
      - "127.0.0.1:59070:9070"
    volumes:
      - restate-data:/restate-data

volumes:
  restate-data:
