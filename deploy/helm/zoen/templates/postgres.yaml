{{- if .Values.reference.enabled }}
{{- if and (eq .Values.postgres.topology.mode "ha") (lt (.Values.postgres.topology.productionReplicas | int) 2) }}
{{- fail "postgres HA topology requires productionReplicas >= 2" }}
{{- end }}
{{- $ha := eq .Values.postgres.topology.mode "ha" }}
{{- $archive := and $ha .Values.postgres.walArchive.enabled }}
{{- if $ha }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: zoen-postgres-start
  labels:
    {{- include "zoen.labels" . | nindent 4 }}
data:
  postgres-start.sh: |
{{ .Files.Get "files/postgres-start.sh" | indent 4 }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: zoen-postgres-ha
  labels:
    {{- include "zoen.labels" . | nindent 4 }}
data:
  primary: postgres-0
---
{{- end }}
apiVersion: v1
kind: Service
metadata:
  name: postgres
  labels:
    {{- include "zoen.labels" . | nindent 4 }}
spec:
  selector:
    app.kubernetes.io/name: postgres
    {{- if $ha }}
    zoen.dev/postgres-role: primary
    {{- end }}
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
      nodePort: {{ .Values.postgres.nodePort }}
  type: NodePort
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres
  labels:
    {{- include "zoen.labels" . | nindent 4 }}
spec:
  serviceName: postgres
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: postgres
      {{- if $ha }}
      zoen.dev/postgres-role: primary
      {{- end }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: postgres
        {{- if $ha }}
        zoen.dev/postgres-role: primary
        {{- end }}
        {{- include "zoen.labels" . | nindent 8 }}
    spec:
      {{- if $archive }}
      initContainers:
        - name: install-wal-g
          image: {{ .Values.images.walG }}
          securityContext:
            runAsUser: 0
          command: [/bin/bash, -c, cp /usr/local/bin/wal-g /wal-g/wal-g && chmod 0755 /wal-g/wal-g]
          volumeMounts:
            - name: wal-g
              mountPath: /wal-g
      {{- end }}
      containers:
        - name: postgres
          image: {{ .Values.images.postgres }}
          {{- if $ha }}
          command: [/bin/bash, /scripts/postgres-start.sh]
          {{- end }}
          env:
            - name: POSTGRES_DB
              value: zoen
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.runtimeSecret.name }}
                  key: {{ .Values.runtimeSecret.postgresAdminPasswordKey }}
            - name: POSTGRES_PROJECTION_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.runtimeSecret.name }}
                  key: {{ .Values.runtimeSecret.postgresProjectionPasswordKey }}
            - name: POSTGRES_USER
              value: postgres
            {{- if $ha }}
            - name: POSTGRES_REPLICATION_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.runtimeSecret.name }}
                  key: {{ .Values.runtimeSecret.postgresReplicationPasswordKey }}
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POSTGRES_ROLE
              value: primary
            - name: PGDATA
              value: /var/lib/postgresql/18/docker
            - name: WAL_ARCHIVE_ENABLED
              value: {{ $archive | quote }}
            - name: WAL_ARCHIVE_TIMEOUT_SECONDS
              value: {{ .Values.postgres.walArchive.archiveTimeoutSeconds | quote }}
            {{- end }}
            {{- if $archive }}
            {{- include "zoen.walGEnv" . | nindent 12 }}
            {{- end }}
          ports:
            - name: postgres
              containerPort: 5432
          readinessProbe:
            exec:
              command: [pg_isready, -U, postgres, -d, zoen]
            initialDelaySeconds: 2
            periodSeconds: 2
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql
            - name: init
              mountPath: /docker-entrypoint-initdb.d
              readOnly: true
            {{- if $ha }}
            - name: start
              mountPath: /scripts
              readOnly: true
            - name: ha
              mountPath: /ha
              readOnly: true
            {{- end }}
            {{- if $archive }}
            - name: wal-g
              mountPath: /wal-g
            {{- end }}
      volumes:
        - name: init
          configMap:
            name: zoen-postgres-init
        {{- if $ha }}
        - name: start
          configMap:
            name: zoen-postgres-start
            defaultMode: 0755
        - name: ha
          configMap:
            name: zoen-postgres-ha
        {{- end }}
        {{- if $archive }}
        - name: wal-g
          emptyDir: {}
        {{- end }}
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: [ReadWriteOnce]
        resources:
          requests:
            storage: {{ .Values.postgres.storage }}
{{- if $ha }}
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-replica
  labels:
    {{- include "zoen.labels" . | nindent 4 }}
spec:
  serviceName: postgres
  replicas: {{ sub (.Values.postgres.topology.productionReplicas | int) 1 }}
  selector:
    matchLabels:
      app.kubernetes.io/name: postgres
      zoen.dev/postgres-role: replica
  template:
    metadata:
      labels:
        app.kubernetes.io/name: postgres
        zoen.dev/postgres-role: replica
        {{- include "zoen.labels" . | nindent 8 }}
    spec:
      containers:
        - name: postgres
          image: {{ .Values.images.postgres }}
          command: [/bin/bash, /scripts/postgres-start.sh]
          args:
            - -c
            - wal_level=replica
            - -c
            - max_wal_senders=16
            - -c
            - max_replication_slots=16
            - -c
            - hot_standby=on
            - -c
            - listen_addresses=*
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POSTGRES_DB
              value: zoen
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.runtimeSecret.name }}
                  key: {{ .Values.runtimeSecret.postgresAdminPasswordKey }}
            - name: POSTGRES_PRIMARY_HOST
              value: postgres
            - name: POSTGRES_REPLICATION_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.runtimeSecret.name }}
                  key: {{ .Values.runtimeSecret.postgresReplicationPasswordKey }}
            - name: POSTGRES_ROLE
              value: replica
            - name: POSTGRES_USER
              value: postgres
            - name: PGDATA
              value: /var/lib/postgresql/18/docker
          ports:
            - name: postgres
              containerPort: 5432
          readinessProbe:
            exec:
              command: [pg_isready, -U, postgres, -d, zoen]
            initialDelaySeconds: 2
            periodSeconds: 2
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql
            - name: start
              mountPath: /scripts
              readOnly: true
            - name: ha
              mountPath: /ha
              readOnly: true
      volumes:
        - name: start
          configMap:
            name: zoen-postgres-start
            defaultMode: 0755
        - name: ha
          configMap:
            name: zoen-postgres-ha
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: [ReadWriteOnce]
        resources:
          requests:
            storage: {{ .Values.postgres.storage }}
{{- end }}
{{- end }}
